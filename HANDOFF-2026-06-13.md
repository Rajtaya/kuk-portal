# UEMS — Handoff (2026-06-13)

**Repo:** `~/Desktop/KUKPortal/kuk-portal/` | GitHub: https://github.com/Rajtaya/UEMS (private)
**Latest commit:** `0edd631`
**Live Frontend:** https://frontend-production-9521.up.railway.app
**Live Backend:** https://backend-production-7615.up.railway.app/api
**Swagger Docs:** https://backend-production-7615.up.railway.app/api/docs
**Railway Dashboard:** https://railway.com/project/b5298461-67be-41a1-a338-5625415a9bfb

---

## What was done today (2026-06-13)

| Commit | Change |
|--------|--------|
| `df77531` → `a656002` | Sanctioned Posts Total = Budgeted + SFS only (no contractual). Filled Total = Budgeted Filled + SFS Filled for arithmetic consistency |
| `ff68522` | Sidebar hides ThemeToggle/DarkModeToggle/SignOut on `/dashboard` (they're in the scope bar instead) |
| `f136e2d` | Sidebar vertical gap fix — removed `flex-1` from `<nav>` |
| `6ed7094` → `4ab4c6c` | Sign Out styled as red gradient box (matches scope bar stat boxes) |
| `604a828` | Dashboard scope bar controls styled as colored boxes — Theme (violet), Dark/Light (slate), Sign Out (red) |
| `916dcba` | Employee filter panel slimmed — `py-3`→`py-1.5`, `gap-5`→`gap-3` |
| `0edd631` | Subject filter added to employee filter panel (fetches from `/masters/subjects`) |

### Dashboard UI/UX overhaul (uncommitted)

| Area | Change |
|------|--------|
| **ChartCard titles** | All chart titles restyled as indigo→blue gradient pills with 4px shadow offset + hover press effect |
| **Default university** | Dashboard loads with "All Universities" selected by default instead of blank |
| **Chart 1 (University bar)** | X-axis shows university codes (KUK, MDU, CDLU…) instead of full names; bolder axis/bar styling |
| **Chart 2 (Hierarchy sunburst)** | 4-level sunburst (Center→Subjects→Designations→Post Types) sorted by total employee count; drill-down via `nodeClick: 'rootToNode'`; counts appear only after drilling (not on default view) — uses ECharts instance API with merge mode to avoid React re-render resetting drill state |
| **Chart 4 (Category sunburst)** | 3-level sunburst (Center: "Category"→Categories→Designations) with same drill-down count logic as hierarchy sunburst |
| **Hierarchy header** | Removed redundant university `<select>` dropdown (scope bar already controls this); subject filter converted to filter icon button with dropdown; "Employee Breakdown" title restyled as gradient pill |
| **Chart 7 (Sanction vs Present)** | Added subject filter icon (same pattern as hierarchy section) — filters which subjects appear on the x-axis |
| **Mobile view** | Scope bar: stat boxes compacted (smaller min-width/padding/font, shorter labels — Unis/Sanct./Filled/Vacant), Sign Out hides label on mobile (icon only). ChartCard title pills: truncated with `text-xs` + `truncate` on mobile, dynamic max-width adjusts for filter icons. Y-axis label shortened to "Count" on mobile. Sanction chart: horizontally scrollable container with 700px min-width on mobile |
| **Technical approach** | Drill-down labels use `useRef` root-tracking stack + `setTimeout(400ms)` → `inst.setOption({...}, false)` merge to add/remove count formatters without triggering React re-renders that reset ECharts' internal drill state |

## What was done yesterday (2026-06-12)

- Filled 255 missing SFS sanctioned posts in both local + Railway DB
- Headline "filled" boxes switched to headcount via `/employees/summary` endpoint (new)
- Dashboard `getDashboardCharts` fixed — `filledPosts = employees.length`
- DB structure docs: `DB_STRUCTURE.md` + `UEMS_DB_Schema.html`

---

## Pending / Known Issues

| Priority | Item | Details |
|----------|------|---------|
| **High** | Dashboard "Sanctioned" stat includes contractual | `getDashboardCharts()` in `employees.service.ts` ~line 350 sums ALL post types. Fix: filter `postType !== 'CONTRACTUAL'` before `.reduce()`. `vacantSeats` corrects automatically. |
| **High** | Employee edit page | No edit form at `/employees/[id]/edit` — only detail view exists |
| **High** | Excel template download | No downloadable `.xlsx` template for bulk upload |
| **High** | Preview before import | Show parsed rows before committing to DB |
| **High** | Duplicate detection | Check `employeeId` uniqueness during upload |
| **High** | Bulk update via Excel | Currently only inserts — should update existing by `employeeId` match |
| **Medium** | Mobile DarkModeToggle duplication | `dashboard/layout.tsx` mobile bar + scope bar both show it |
| **Medium** | Master data management UI | No frontend CRUD for Subject/Designation masters |
| **Medium** | Designation dropdown filter | Add to employee list filter bar (Subject filter done in `0edd631`) |
| **Medium** | Audit logging for uploads | Bulk upload events not tracked specifically |
| **Low** | Password reset flow | Must be done by Super Admin — no self-service |
| **Low** | University CRUD UI | Add/edit universities from frontend (API exists, no UI) |
| **Low** | Dark mode for detail/form pages | `/employees/[id]`, `/new`, `/upload`, login not dark-themed |

---

## Key Architecture Notes

- **Monorepo:** `backend/` (NestJS 11) + `frontend/` (Next.js 15.5, React 19)
- **Auth:** JWT + RBAC — 3 roles (SUPER_ADMIN, STATE_USER, UNIVERSITY_ADMIN)
- **DB:** PostgreSQL — local `uems_local` (`postgresql://aarya@localhost:5432/uems_local`), Railway prod
- **Prisma pinned to 6.19.3** — always use `./node_modules/.bin/prisma`, never bare `npx prisma`
- **Filled numbers rule:** Total Filled = Budgeted Filled + SFS Filled (contractual excluded from sanctioned posts page, shown on employees page only)
- **Controls placement:** Dashboard scope bar has ThemeToggle/DarkModeToggle/SignOut; sidebar hides them on `/dashboard` via `isDashboard` check. All other pages show them in sidebar.

## Seed Logins (password: `admin123`)

| Role | Email |
|------|-------|
| Super Admin | admin@he.haryana.gov.in |
| State User | state@he.haryana.gov.in |
| KUK Admin | admin@kuk.ac.in |
| MDU Admin | admin@mdu.ac.in |
| CDLU Admin | admin@cdlu.ac.in |
