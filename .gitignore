node_modules
dist
.next
.next 2/
.env
.env*.local
*.tsbuildinfo
*.log
uploads/
.DS_Store
.claude/
# package-lock.json is intentionally committed: it pins exact, integrity-checked
# dependency versions so `npm ci` produces reproducible, tamper-evident builds.
